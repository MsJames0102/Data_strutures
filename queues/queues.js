class Node { // a Class build the object its arguementee
    constructor(data) { // function that product these object are constructor
        this.data = data // data equal data
        this.next = null // no next
    }
}
